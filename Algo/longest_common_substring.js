function longestCommonSubstring(str1, str2) {
    const m = str1.length;
    const n = str2.length;

    for (let i = 0; i < m; i++) {
        for (let j = 0; j < n; j++) {
            if(str1[i] === str2[j]){
                
            }
        }
      }
}
  
  const str1 = "abcdefg";
  const str2 = "cdefghi";
  const result = longestCommonSubstring(str1, str2);
  
  console.log(result); 